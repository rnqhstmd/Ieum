app:
  invitation:
    expiry-cron: "-"
  resend:
    api-key: ""
    from: "Ieum <noreply@test.local>"

spring:
  flyway:
    enabled: true

  jpa:
    hibernate:
      ddl-auto: validate

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: test-client-id
            client-secret: test-client-secret
            scope:
              - profile
              - email
